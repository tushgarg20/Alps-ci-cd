#Unit	GSIM residency			Comments
#Instance Counts				
        .CrClk	=	D(MAX_CYCLES)
        num_ROW_ROW              =   D(knob.numROW)
        BF_Flag                 =     D(knob.DPAS_BF16) 
        SYS_Utilization          =     (BF_Flag == 0) ? D(SYS_FLOAT)/CrClocks : D(SYSW_FLOAT)/CrClocks
        EU_Utilization          =   (SYS_Utilization == 0) ? D(FPU0_UTILIZATION): SYS_Utilization 

#Generic
        PS2_CAM_ROW        =  EU_Utilization
